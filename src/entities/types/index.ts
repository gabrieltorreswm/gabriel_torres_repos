export enum verificationCode {
    VERIFIED = 604,
    PENDING = 605,
    APPROVED = 606
}

export enum repositoryState {
    ENABLE = "Enable",
    DISABLE = "Disable",
    ARCHIVED = "Archived"
}

export enum repositoryStatus {
    ACTIVE = "Active",
    INACTIVE = "Inactive",
}